{{#> layouts/email_base }}
    {{#* inline "body"}}
        <div class="title">
            <h3>You have temporarily reserved an appointment.</h3>
        </div>
        <div class="description with-button">
            <button class="button">
                <a href="{{{user_portal_url '/payment/invoice' }}}/{{invoice}}" target="_blank">
                    <span><font color="#ffffff">Proceed to payment</font></span>
                </a>
            </button>
        </div>
        {{#> testimonials}}
            {{> blocks/testimonials }}
        {{/testimonials}}
    {{/ inline}}
{{/ layouts/email_base}}
